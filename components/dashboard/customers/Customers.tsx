import CustomersHeader from '@/components/dashboard/customers/customers-header/CustomersHeader';

type Props = {};

export default function Customers({}: Props) {
  return (
    <>
      <CustomersHeader />
    </>
  );
}
